# Screenshots

These images are shown on the landing page (https://azmawee.github.io/EmperorReborn/)
and are indexed by Google Images, so good screenshots help SEO.

Replace any placeholder with a real screenshot, keeping the exact same filename so
the page and its alt text keep working:

| File | Show | Status |
|---|---|---|
| `gameplay-windows11.png` | The game running at an original resolution on Windows 11 | real |
| `launcher-ui.png` | The Emperor Reborn launcher with the resolution / graphics options | real |
| `multiplayer-connect.png` | The multiplayer connect screen (direct IP and DDNS hostname) | not on the page yet; add the image, then re-add it to `index.md` |

Recommended: PNG, 1280x720 or the native game resolution. Keep file size reasonable
(under ~500 KB each) so the page loads fast.
