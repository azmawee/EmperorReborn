# Screenshots

These images are shown on the landing page (https://azmawee.github.io/EmperorReborn/)
and are indexed by Google Images, so good screenshots help SEO.

The three PNGs here are temporary placeholders. Replace each one with a real
screenshot, keeping the exact same filename so the page and its alt text keep working:

| File | Show |
|---|---|
| `gameplay-windows11.png` | The game running fullscreen on Windows 11 at an original resolution |
| `launcher-ui.png` | The Emperor Reborn launcher with the resolution / graphics options |
| `multiplayer-connect.png` | The multiplayer connect screen (direct IP and DDNS hostname) |

Recommended: PNG, 1280x720 or the native game resolution. Keep file size reasonable
(under ~500 KB each) so the page loads fast.
